import { CreateBaseStrategy } from '@sudoku/strategy';
import { strategyGrid } from '@sudoku/stores';

export class HiddenSingleStrategy extends BaseStrategy {
  constructor(description = "Hidden Single", priority = 1) {
    super();
    this.description = description;
    this.priority = priority;
  }

  preCondition(grid) {
    return true;
  }

  apply(grid, strategyApplyCell) {

  }

  strategyDescription() {
    return this.description;
  }
}