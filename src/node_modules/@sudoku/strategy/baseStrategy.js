import { BOX_SIZE, GRID_LENGTH, SUDOKU_SIZE, GRID_COORDS } from '@sudoku/constants';
import { writable } from 'svelte/store';

export const branchCounter = writable(0);

function CreateBaseStrategy() {
  return {
    preCondition: (grid) => {
      throw new Error("Base strategy is just an interface.");
    },

    apply: (grid) => {
      throw new Error("Base strategy is just an interface.");
    },

    strategyDescription: () => {
      throw new Error("Base strategy is just an interface.");
    }
  }
}



